# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
.next/
out/
build/

# Testing (root only: `src/app/api/.../knowledge/coverage` is a route, not a report)
/coverage/

# Production
dist/

# MapLibre's worker, copied out of node_modules by `npm run maplibre:worker` (predev/prebuild)
/public/maplibre/

# Misc
.DS_Store
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Supabase
supabase/.temp/

# Test results
test-results/
playwright-report/
.playwright-mcp/

# Local screenshots / scratch artifacts
screenshots/

.next-dev/

# Local AppView uploads (production uses the uploads volume)
.uploads/
deploy/local/pds.env

# Local embeddings model cache (production bakes it into the image at /models)
.models/

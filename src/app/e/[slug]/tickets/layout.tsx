import { EventUtilityLayout } from '@/components/EventUtilityLayout'
export default function TicketLayout({ children }: { children: React.ReactNode }) {
  return <EventUtilityLayout>{children}</EventUtilityLayout>
}
